apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bank-demo-cert
  namespace: bank-demo
spec:
  secretName: bank-demo-cert-tls
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  commonName: bank-demo.bunnana.dev
  dnsNames:
  - bank-demo.bunnana.dev
