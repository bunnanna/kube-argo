apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-cert
  namespace: vault
spec:
  secretName: vault-cert-tls
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  commonName: vault.bunnana.dev
  dnsNames:
  - vault.bunnana.dev