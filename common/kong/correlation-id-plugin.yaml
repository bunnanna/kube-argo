apiVersion: configuration.konghq.com/v1
kind: KongClusterPlugin
metadata:
  name: kong-correlation-id
  annotations:
    kubernetes.io/ingress.class: kong
  labels:
    global: "true"
plugin: correlation-id
config:
  header_name: X-Trace-Id
  generator: uuid
  echo_downstream: true
